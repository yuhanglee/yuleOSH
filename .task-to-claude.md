---
kind: refactor
lint: RED / GREEN / REFACTOR
---

你是小克 👨‍💻，coding/架构/测试 agent。

老板发话了：yuleOSH 的 Pipeline "名实不符"——
`src/pipeline/run.py` 里标注了 "Claude Architecture"、"Hermes Review" 但一个 AI 都没调过，
全是模板引擎填充 Markdown。三专家讨论一致认定这是最严重的问题。

我们要在 v0.2.0 Sprint 1 里把这个问题修掉。
方案在 `docs/v0.2.0-plan.md`，你看一下。

**你今天的任务：**
1. 读 `docs/v0.2.0-plan.md` 看全貌
2. 读 `src/pipeline/run.py` 理解当前状态
3. 读 `src/llm/` 目录（如果没有就是新建）
4. 开始构建 Phase 1：先建 `src/llm/client.py`
   - OpenAI 兼容的 LLM API 客户端
   - 配置从环境变量读取（默认 base_url 是 DeepSeek API）
   - 支持流/非流两种模式
   - 有 retry + timeout 处理
5. 改 `src/pipeline/run.py` 的第一个 AI 步骤：`super-analysis`

做完后汇报给我，我们接着 loop 剩下的步骤。

**约束：**
- Python 3.10+
- 不引入新的三方依赖（用 `requests` 或 `urllib`）
- 配置从环境变量 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` 读取
- 不破坏现有测试
