# yuleOSH v0.2.0 — Acceptance Matrix

> **Sprint**: v0.2.0 Sprint 1 — AI Pipeline 诚实化
> **Author**: Hermes (质量架构)
> **Date**: 2026-06-08

## 验证范围

本矩阵覆盖 v0.2.0 Sprint 1 的所有验收项：
1. LLM Client 基础设施 (`src/llm/client.py`)
2. Pipeline AI 化改造（5 个步骤）
3. 单元测试覆盖
4. 端到端验收

---

## 1. LLM Client 基础设施

| # | 验收条件 | 验收方法 | 状态 | 验证详情 |
|---|---------|---------|------|---------|
| 1.1 | 支持 OpenAI 兼容 API | 代码审查 + 单元测试 | ✅ PASS | `_build_request` 构建标准 OpenAI-compatible POST 请求到 `/v1/chat/completions`，`test_has_correct_url` 验证端点格式 |
| 1.2 | 从环境变量读取配置 | 单元测试 | ✅ PASS | `LLM_API_KEY` > `DEEPSEEK_API_KEY` > `OPENAI_API_KEY` 三级 fallback，`LLM_BASE_URL` 默认 `https://api.deepseek.com`，`LLM_MODEL` 默认 `deepseek-chat` |
| 1.3 | 重试 + 退避机制 | 单元测试 | ✅ PASS | `retries` 参数，指数退避 `1s → 2s → 4s`，`test_backoff_doubles` 验证退避间隔 |
| 1.4 | 超时处理 | 单元测试 | ✅ PASS | `timeout` 参数传递给 `urllib.request.urlopen`，`test_timeout_passed_to_urlopen` 验证 |
| 1.5 | HTTP 错误处理 | 单元测试 | ✅ PASS | HTTP 400/401/429/503 等错误 → `RuntimeError`，含错误消息解析 |
| 1.6 | 无效 JSON 处理 | 单元测试 | ✅ PASS | 响应 `not json` → `RuntimeError("invalid JSON")` |
| 1.7 | null content 处理 | 单元测试 | ✅ PASS | LLM 拒绝回复时 content=null → 降级消息 `[LLM refused, finish_reason=...]` |
| 1.8 | 空 choices 处理 | 单元测试 | ✅ PASS | `choices: []` 或缺失 `choices` key → `RuntimeError("Unexpected API response")` |
| 1.9 | 无 API key 保护 | 单元测试 | ✅ PASS | 环境变量全空 → `RuntimeError("No LLM API key found")` |
| 1.10 | 无需第三方依赖 | 文件检查 | ✅ PASS | 仅使用 `urllib` + `json`，无 `requests`/`httpx`/`openai` |

**测试统计**: 36 个单元测试全部通过

---

## 2. Pipeline AI 化改造

### 2.1 super-analysis → LLM

| # | 验收条件 | 验证方式 | 状态 | 详情 |
|---|---------|---------|------|------|
| 2.1.1 | 步骤 2 调用 `chat_completion` | 代码审查 | ✅ | `step_super_analysis` 调用 `chat_completion(system_prompt, user_prompt)` |
| 2.1.2 | 输入包含完整 spec + 元数据 | 代码审查 | ✅ | 前 12K chars spec content + requirements/scenarios 计数 |
| 2.1.3 | 输出为格式化 Markdown | 代码审查 | ✅ | 带 token usage metadata header |
| 2.1.4 | 存储到 session session_dir | 代码审查 | ✅ | `startup-analysis.md` |

### 2.2 hermes-prd → LLM

| # | 验收条件 | 验证方式 | 状态 | 详情 |
|---|---------|---------|------|------|
| 2.2.1 | 步骤 3 调用 `chat_completion` | 代码审查 | ✅ | `step_hermes_prd` 调用 LLM |
| 2.2.2 | 输入包含 spec + S.U.P.E.R 分析 | 代码审查 | ✅ | 从 artifacts 读取 super-analysis |
| 2.2.3 | 输出包含 PRD 标准结构 | 代码审查 | ✅ | Product Overview, User Stories, Acceptance Criteria, Risks |
| 2.2.4 | 优先级标注 (P0/P1/P2) | 代码审查 | ✅ | prompt 要求 LLM 分配优先级 |

### 2.3 claude-arch → LLM

| # | 验收条件 | 验证方式 | 状态 | 详情 |
|---|---------|---------|------|------|
| 2.3.1 | 步骤 5 调用 `chat_completion` | 代码审查 | ✅ | `step_claude_arch` 调用 LLM |
| 2.3.2 | 输入包含项目目录结构 | 代码审查 | ✅ | 自动 `os.walk(src)` 扫描文件树 + 关键文件 snippet |
| 2.3.3 | 输出包含 ADR、Bounded Context | 代码审查 | ✅ | prompt 要求架构设计文档包含 ADR |
| 2.3.4 | 引用真实文件路径 | 代码审查 | ✅ | prompt 传入实际 relpath |

### 2.4 claude-dev → LLM

| # | 验收条件 | 验证方式 | 状态 | 详情 |
|---|---------|---------|------|------|
| 2.4.1 | 步骤 6 调用 `chat_completion` | 代码审查 | ✅ | `step_claude_dev` 调用 LLM |
| 2.4.2 | 输入包含 arch + PRD + SUPER | 代码审查 | ✅ | 从 artifacts 读取三个前置输出 |
| 2.4.3 | 输出包含任务分解 + 技术债务 | 代码审查 | ✅ | prompt 要求 task breakdown + tech debt |
| 2.4.4 | 输出包含风险分析 | 代码审查 | ✅ | Risk Assessment 部分 |

### 2.5 hermes-review → LLM

| # | 验收条件 | 验证方式 | 状态 | 详情 |
|---|---------|---------|------|------|
| 2.5.1 | 步骤 8 调用 `chat_completion` | 代码审查 | ✅ | `step_hermes_review` 调用 LLM |
| 2.5.2 | 输入包含源码 + 所有制品 | 代码审查 | ✅ | 读取 spec + artifacts + 扫描 .py 源码文件 |
| 2.5.3 | 输出为结构化 JSON | 代码审查 | ✅ | 解析 LLM JSON 输出，含 severity/category/file/line/message |
| 2.5.4 | JSON 解析容错 | 代码审查 | ✅ | 包含 markdown fence 剥离 + JSONDecodeError fallback |
| 2.5.5 | critical 发现阻断 pipeline | 代码审查 | ⏳ | 当前仅记录，未阻断；建议后续 Sprint 实现 |

---

## 3. 单元测试覆盖

### 3.1 LLM Client 测试

| 测试类 | 测试数 | 覆盖率维度 | 状态 |
|-------|-------|-----------|------|
| `TestResolveEnv` | 9 | API key 优先级、base URL、model、空 key、自定义参数 | ✅ |
| `TestBuildPayload` | 4 | payload 结构、JSON 格式、message 层级、Unicode | ✅ |
| `TestBuildRequest` | 5 | URL、认证头、Content-Type、POST 方法、请求体 | ✅ |
| `TestDoRequest` | 5 | 成功响应、HTTP 错误、无 body 错误、连接错误、无效 JSON | ✅ |
| `TestChatCompletion` | 12 | 基本调用、无 key、null content、空 choices、重试、超时、端点、退避 | ✅ |
| 日志测试 | 1 | LLM 调用日志记录 | ✅ |
| **合计** | **36** | **完整覆盖所有关键路径** | ✅ |

### 3.2 其他模块测试

| 测试文件 | 测试数 | 状态 |
|---------|-------|------|
| `test_ci_engine.py` | 5 | ✅ |
| `test_e2e.py` | 10 (8 pass + 2 skip) | ✅ |
| `test_evidence_engine.py` | 4 | ✅ |
| `test_perf.py` | 2 | ✅ |
| `test_review_engine.py` | 5 | ✅ |
| `test_spec_engine.py` | 4 | ✅ |
| `test_spec_engine_extended.py` | 8 | ✅ |
| `test_store.py` | 6 | ✅ |
| **合计** | **79 pass | 2 skip** | ✅ |

---

## 4. 端到端验收

| # | 验收条件 | 验证方法 | 状态 | 备注 |
|---|---------|---------|------|------|
| 4.1 | Spec 验证 E2E | `test_e2e_spec_validate` | ✅ PASS | 验证 `docs/spec.md` ≥7 requirements，覆盖率过阈值 |
| 4.2 | Spec diff E2E | `test_e2e_spec_diff` | ✅ PASS | 相同文件 diff = 0 变更 |
| 4.3 | Pipeline 结构验证 | `test_e2e_pipeline_spec_check_only` | ✅ PASS | spec-check 步骤独立可运行，无需 API key |
| 4.4 | Pipeline 全流程 E2E | `test_e2e_pipeline_run` | ⏸️ SKIP | 需 `LLM_API_KEY` 环境变量；有 key 时完整运行 9 步流水线 |
| 4.5 | Pipeline 状态查询 | `test_e2e_pipeline_status` | ✅ PASS | `run.py status` 正常响应 |
| 4.6 | 证据包生成 | `test_e2e_evidence_generate` | ✅ PASS | 生成 `compliance-pack.zip` |
| 4.7 | 自动审查 | `test_e2e_review_auto` | ✅ PASS | `run.py auto` 不崩溃 |
| 4.8 | CLI 帮助 | `test_e2e_cli_help` | ✅ PASS | `yuleosh.sh help` 显示用法 |
| 4.9 | Dashboard API | `test_e2e_dashboard_server` | ✅ PASS | 8080 端口 status API 正常 |

---

## 5. 剩余工作 / 已知不足

| # | 项目 | 优先级 | 说明 |
|---|------|--------|------|
| 5.1 | Pipeline 端到端自动化测试 | P1 | 需要 mock API key 或专用的 CI 环境来跑完整流水线 |
| 5.2 | JSON review 阻断机制 | P1 | hermes-review 发现 critical 发现时应阻断 pipeline，当前仅记录 |
| 5.3 | 步骤间制品缓存复用 | P2 | 当前同 session 内 artifact 串行传递没问题，跨 session 未支持 |
| 5.4 | 流式 (streaming) 支持 | P2 | client.py 当前仅支持非流模式 |
| 5.5 | Token 用量预估 | P2 | 大型 spec (＞12K chars) 会被截断，需智能 chunk |
| 5.6 | 多 Agent 并行 | P3 | Claude 和 Hermes 可并行运行以加速 pipeline |

---

## 6. 总体评分

| 维度 | 评分 | 说明 |
|------|------|------|
| LLM Client 质量 | 🟢 A | 36 个单元测试覆盖所有路径，错误处理完善 |
| Pipeline AI 化 | 🟢 A | 5 个 AI 步骤全部接入 LLM，prompt 设计合理 |
| 测试覆盖 | 🟢 A | 79 测试通过，新增 36 个 LLM Client 测试 |
| E2E 验收 | 🟡 B | 非 API key 依赖的测试全部通过，全流程待 CI 环境 |
| 文档完整性 | 🟢 A | 本矩阵 + Final Report 完整记录 Sprint 成果 |
