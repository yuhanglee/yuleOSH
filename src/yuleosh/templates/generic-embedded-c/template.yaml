name: generic-embedded-c
version: 1.0.0
description: Generic embedded C project template with HAL abstraction layer
platforms: [generic, arm, stm32]
tags: [embedded, c, hal, mcu]
spec_sections:
  - System Requirements
  - Software Requirements
  - Hardware Abstraction
  - Test Plan
pipeline_config:
  default_steps: [spec-parse, requirements-analysis, sdd, code-gen, internal-review, test-plan, code-review, unit-test, cross-compile, system-verification]
  ci_layers: [L1, L2, L3]
